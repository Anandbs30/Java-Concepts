package V36_Collection_FrameWork.Collection_Interface.TimeComplexity;

public class Time_Complexity_Calculation {
	/*
	 * Adding by index (get) is O(1).
	 * Adding an element is O(n) in the worst case when resizing occurs.
	 * Removing elements can be O(n) because it may involve shifting elements.
	 * Iteration is O(n). 
	 */
}
